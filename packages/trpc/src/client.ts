import type { AppRouter } from "./root";

/** Re-exported for client packages to build typed callers/links. */
export type { AppRouter };
export type { inferRouterInputs, inferRouterOutputs } from "@trpc/server";
